# RSS Reader 環境変数設定例
# このファイルをコピーして .env ファイルを作成してください
# 注意: .env ファイルは .gitignore に含まれており、Gitにコミットされません

# ============================================
# バックエンド環境変数
# ============================================

# DynamoDB テーブル名
DYNAMODB_TABLE_NAME=rss-reader-dev

# AWS Bedrock 設定
BEDROCK_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-2-multimodal-embeddings-v1:0
EMBEDDING_DIMENSION=1024

# API 認証キー（強力なランダム文字列を使用してください）
# 生成例: openssl rand -base64 32
API_KEY=your-secure-api-key-here-change-this

# CORS 許可オリジン（カンマ区切り）
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# 環境識別子
ENVIRONMENT=development

# セキュリティ設定
SECURITY_HEADERS_ENABLED=true
LOG_LEVEL=INFO

# ============================================
# フロントエンド環境変数
# ============================================

# バックエンドAPI URL
VITE_API_URL=http://localhost:8000

# API 認証キー（バックエンドと同じ値を使用）
VITE_API_KEY=your-secure-api-key-here-change-this

# ============================================
# AWS CDK デプロイ用環境変数
# ============================================

# AWSアカウントID（自動取得する場合は不要）
# CDK_DEFAULT_ACCOUNT=$(aws sts get-caller-identity --query Account --output text)
CDK_DEFAULT_ACCOUNT=123456789012

# AWSリージョン
CDK_DEFAULT_REGION=ap-northeast-1

# RSS Reader API Key（デプロイ時に必須）
RSS_READER_API_KEY=your-secure-api-key-here-change-this

# ============================================
# AWS認証情報（GitHub Actions CI/CD用）
# ============================================
# 注意: ローカル開発では AWS CLI の認証情報を使用してください
# GitHub Secretsに設定する場合のみ使用

# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ============================================
# セキュリティ注意事項
# ============================================
# 1. API_KEY は強力なランダム文字列を使用してください
#    生成例: openssl rand -base64 32
# 2. 本番環境では AWS Secrets Manager または Parameter Store の使用を推奨
# 3. .env ファイルは絶対に Git にコミットしないでください
# 4. GitHub Actions では GitHub Secrets を使用してください

